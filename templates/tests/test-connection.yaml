apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tv-sso.fullname" . }}-test-connection"
  labels:
    {{- include "tv-sso.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tv-sso.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
