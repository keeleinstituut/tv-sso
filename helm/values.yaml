# Default values for tv-sso.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  projectName: tv

keycloak:
  nameOverride: sso
  image:
    repository: "__IMAGE_NAME__" # Replaced during continuous delivery pipeline
    tag: "__GIT_COMMIT_SHA__" # Replaced during continuous delivery pipeline
    pullPolicy: Always
  ingress:
    enabled: true
    # TODO: Update configuration below after adopting Istio
    hostname: sso.test.tolkevarav
    path: /(.*)
    pathType: Prefix
    annotations:
      nginx.ingress.kubernetes.io/rewrite-target: /$1
      nginx.ingress.kubernetes.io/use-regex: "true"
  # TODO: Once Postgres operator is in use, use the operator’s database as suggested by commented-out code below
#  postgresql.enabled: "false"
#  externalDatabase:
#    host: ""
#    port: ""
#    database: ""
#    user: ""
#    existingSecret: ""

